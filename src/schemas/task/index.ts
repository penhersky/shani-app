import {getMy, getOrder} from './get';
import {create} from './create';
import {cancelPerformer, performedRefused, setStatus} from './statuses';

export default {
  getMy,
  getOrder,
  create,
  cancelPerformer,
  performedRefused,
  setStatus,
};
